<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.lostbutfound">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LostButFound"
        tools:targetApi="31">
        <activity
            android:name=".ThingPage"
            android:exported="false" />
        <activity
            android:name=".AddThings"
            android:exported="false" />
        <activity
            android:name=".Yandex"
            android:exported="false" /> <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="AIzaSyBIKPUIEjfTpJXbIahj2dHnuA_3jpbKM6o" /> -->
        <activity
            android:name=".FirstActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="${AIzaSyDgjoCyGDu5RSKjqeMPfTGycRwaOjV771M}" /> -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.gms.version" -->
        <!-- android:value="@integer/google_play_services_version" /> -->
        <activity
            android:name=".itemPageView.AnimalsPageView"
            android:exported="false" />
        <activity
            android:name=".Add.AddRelativesStatement"
            android:exported="false" />
        <activity
            android:name=".Add.AddAnimalsStatements"
            android:exported="false" />
        <activity
            android:name=".Search"
            android:exported="false" />
        <activity
            android:name=".itemPageView.HumanPageView"
            android:exported="false" />
        <activity
            android:name=".User.UserPage"
            android:exported="false" />
        <activity
            android:name=".Add.AddPeopleStatements"
            android:exported="false" />
        <activity
            android:name=".MainActivity2"
            android:exported="false" />
        <activity
            android:name=".User.CreateAccount"
            android:exported="false" />
        <activity
            android:name=".User.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>
    </application>

</manifest>